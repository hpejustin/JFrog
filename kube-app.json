{
  "apiVersion": "extensions/v1beta1",
  "kind": "Deployment",
  "metadata": {
    "name": "jfrog-cloud-app"
  },
  "spec": {
    "replicas": 2,
    "template": {
      "metadata": {
        "labels": {
          "app": "jfrog-cloud-app"
        }
      },
      "spec": {
        "containers": [
          {
            "name": "jfrog-cloud-app",
            "image": "docker-release-local2.demo.jfrogchina.com/jfrog-cloud-demo:{tag}",
            "imagePullPolicy": "Always",
            "ports": [
              {
                "containerPort": 8080
              }
            ]
          }
        ]
      }
    }
  }
}
